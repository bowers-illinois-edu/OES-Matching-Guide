.Rproj.user
.Rhistory
.RData
.Ruserdata

# macOS
.DS_Store

# Node (only used for the remark markdown linter)
node_modules/

# Quarto intermediate/build files
/.quarto/
*_files/
*.knit.md

# Rendered site: built and published by the GitHub Action, not committed.
# (The Action renders from the committed _freeze/ results, so _freeze/ is
# intentionally NOT ignored.)
/docs/

# Large replication data: exceeds GitHub's 100 MB per-file limit; keep out of the repo
replication_file.zip
replication_file/

**/*.quarto_ipynb
.positai
